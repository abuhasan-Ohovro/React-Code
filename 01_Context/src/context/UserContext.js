import React from "react";

const UserContext = React.createContext()


export default UserContext;

// Every context api work with the help of a provider. Provider work as a data and network call provider to the context. Now we will see how to create a provide :)

